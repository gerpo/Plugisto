<?php

return [
    /*
     * Loads the predefined plugisto routes. Allows for basic plugin management.
     */
    'auto_load_routes' => true,

    /*
     * If packages include a install command it will be run automatically on installation.
     * If you want to install the packages manually set to false.
     */
    'auto_install' => true,
];
